"use client";import { motion } from "framer-motion";import { Building2,BriefcaseBusiness,Sparkles } from "lucide-react";import type { CorporateHoroscopeResult } from "@/lib/oracle/types";
const icons={candidate:Sparkles,company:Building2,future:BriefcaseBusiness};
export function TarotReveal({cards}:{cards:CorporateHoroscopeResult["tarotCards"]}){return <div className="grid gap-3 md:grid-cols-3" aria-label="Three-card corporate tarot reading">{cards.map((card,i)=>{const Icon=icons[card.position];return <motion.article key={card.position} className="relative min-h-[230px] overflow-hidden rounded-xl border border-[#c7c3b8] bg-[#fffdf8] p-5 shadow-sm" initial={{opacity:0,y:24,rotateY:75}} animate={{opacity:1,y:0,rotateY:0}} transition={{delay:i*.28,duration:.5}}><span className="absolute right-3 top-3 font-mono text-[9px] text-[#999]">0{i+1}</span><div className="grid h-12 w-12 place-items-center rounded-full border border-[#c9bb9a] bg-[#f7f0df] text-[#705b2e]"><Icon size={21}/></div><p className="mt-5 text-[9px] font-bold uppercase tracking-[.15em] text-[#777]">{card.subtitle}</p><h3 className="mt-1 text-lg font-semibold">{card.title}</h3><p className="mt-3 text-[11px] leading-5 text-[#555]">{card.meaning}</p><span className="absolute bottom-2 left-1/2 -translate-x-1/2 text-[#c1aa79]">✦</span></motion.article>})}</div>}
