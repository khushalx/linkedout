"use client";import Link from "next/link";import { RotateCcw,Trash2 } from "lucide-react";import type { SavedReading } from "@/lib/oracle/types";
export function SavedReadings({readings,onDelete,onClear}:{readings:SavedReading[];onDelete:(id:string)=>void;onClear:()=>void}){return <section className="card p-4"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Saved compatibility readings</h2><p className="text-[10px] text-[#666]">Career astrology with an audit trail.</p></div>{readings.length>0&&<button onClick={onClear} className="flex items-center gap-1 text-[10px] font-bold text-[#777]"><RotateCcw size={12}/>Clear</button>}</div>{readings.length?<div className="mt-3 space-y-2">{readings.slice(0,4).map(r=><div key={r.id} className="flex items-center gap-3 rounded-lg border border-[#ddd] p-3"><span className="grid h-9 w-9 place-items-center rounded-full bg-[#edf3f8] text-[12px] font-black text-[#0A66C2]">{r.compatibilityScore}</span><div className="min-w-0 flex-1"><Link href={`/oracle?job=${r.jobId}&reading=${r.id}`} className="text-[11px] font-semibold hover:text-[#0A66C2] hover:underline">{r.role} · {r.company}</Link><p className="truncate text-[9px] text-[#777]">{r.humorousVerdict}</p></div><button onClick={()=>onDelete(r.id)} aria-label={`Delete ${r.role} reading`}><Trash2 size={14} className="text-[#777]"/></button></div>)}</div>:<p className="mt-4 rounded bg-[#f5f5f3] p-4 text-center text-[11px] text-[#666]">No readings saved. The planets have filed no paperwork.</p>}</section>}
