"use client";import Link from "next/link";import { Bookmark,MapPin,Sparkles } from "lucide-react";import type { CompanyProfile,JobProfile } from "@/lib/oracle/types";
export function JobCard({job,company,saved,onToggleSave}:{job:JobProfile;company:CompanyProfile;saved:boolean;onToggleSave:()=>void}){return <article className="card p-4"><div className="flex gap-3"><span className="grid h-12 w-12 flex-none place-items-center rounded-lg bg-[#e9f1f8] font-black text-[#0A66C2]">{company.logoInitials}</span><div className="min-w-0 flex-1"><h2 className="text-[14px] font-semibold text-[#0A66C2]">{job.title}</h2><p className="text-[12px]">{company.name}</p><p className="mt-1 flex items-center gap-1 text-[10px] text-[#666]"><MapPin size={12}/>{job.location} · {job.workplace.replace("_","-")}</p></div><button onClick={onToggleSave} className="icon-button h-8 w-8" aria-label={saved?"Unsave job":"Save job"}><Bookmark size={18} fill={saved?"currentColor":"none"}/></button></div><div className="mt-3 flex flex-wrap gap-1">{job.skills.slice(0,3).map(s=><span key={s} className="rounded-full bg-[#f0f0ee] px-2 py-1 text-[9px] font-semibold">{s}</span>)}</div><p className="mt-3 text-[10px] text-[#057642]">Actively reviewing · Posted {job.postedDaysAgo}d ago</p><div className="mt-3 flex items-center gap-2 border-t border-[#eee] pt-3"><Link href={`/oracle?job=${job.id}`} className="outline-button flex items-center gap-1 px-3 py-1.5 text-[11px]"><Sparkles size={13}/>Check compatibility</Link>{job.salaryLabel&&<span className="ml-auto text-[10px] font-semibold text-[#666]">{job.salaryLabel}</span>}</div></article>}
