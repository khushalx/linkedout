"use client";
import { useState } from "react"; import { Avatar } from "@/components/shared/avatar";
export function CommentSection(){const [value,setValue]=useState("");const [comments,setComments]=useState<string[]>([]);function submit(e:React.FormEvent){e.preventDefault();if(value.trim()){setComments(c=>[...c,value.trim()]);setValue("")}}return <div className="border-t border-[#eee] px-4 py-3">{comments.map((c,i)=><div key={i} className="mb-3 flex gap-2"><Avatar initials="KD" size={30}/><div className="rounded-xl bg-[#f2f2f2] px-3 py-2 text-[13px]"><b>Khushal Dangar</b><p>{c}</p></div></div>)}<form onSubmit={submit} className="flex gap-2"><Avatar initials="KD" size={32}/><label className="flex-1"><span className="sr-only">Write a comment</span><input value={value} onChange={e=>setValue(e.target.value)} placeholder="Add a comment…" className="w-full rounded-full border border-[#999] px-4 py-2 text-[13px]"/></label><button className="text-[13px] font-semibold text-[#0A66C2]" disabled={!value.trim()}>Post</button></form></div>}
