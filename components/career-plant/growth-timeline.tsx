import { BadgeCheck,BookOpen,FolderCheck,HeartHandshake,MessageSquareText,Plus } from "lucide-react";import type { GrowthEntry } from "@/lib/career/types";
const icons={project:FolderCheck,skill:Plus,verification:BadgeCheck,endorsement:HeartHandshake,post:MessageSquareText,learning:BookOpen};
export function GrowthTimeline({entries}:{entries:GrowthEntry[]}){return <section className="card p-5 sm:p-6"><h2 className="text-lg font-semibold">Growth history</h2><p className="mt-1 text-[12px] text-[#666]">Professional activity, translated into botanical paperwork.</p><ol className="mt-5">{entries.map((entry,i)=>{const Icon=icons[entry.type];return <li key={entry.id} className="relative flex gap-3 pb-5 last:pb-0"><span className="z-10 grid h-8 w-8 flex-none place-items-center rounded-full border border-[#c8d8c5] bg-[#f3f8f1] text-[#39734a]"><Icon size={15}/></span>{i<entries.length-1&&<span className="absolute left-[15px] top-8 h-[calc(100%-20px)] w-px bg-[#d7ddd4]"/>}<div><p className="text-[13px] font-semibold">{entry.text}</p><time className="text-[10px] text-[#777]">{new Date(entry.date).toLocaleDateString("en-IN",{day:"numeric",month:"short",year:"numeric"})}</time></div></li>})}</ol></section>}
