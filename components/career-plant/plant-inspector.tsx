import { BadgeCheck,GitBranch,Flower2,Sprout } from "lucide-react";import type { PlantSelection } from "./career-plant-svg";
export function PlantInspector({selection,onClose}:{selection:PlantSelection|null;onClose:()=>void}){const Icon=selection?.kind==="leaf"?BadgeCheck:selection?.kind==="branch"?GitBranch:selection?.kind==="flower"?Flower2:Sprout;return <section className="rounded-lg border border-[#d7dfd4] bg-[#f8fbf7] p-4" aria-live="polite"><div className="flex items-start gap-3"><span className="grid h-9 w-9 place-items-center rounded-full bg-[#e1efe1] text-[#39734a]"><Icon size={18}/></span><div className="min-w-0 flex-1"><h3 className="text-[13px] font-bold">{selection?.title||"Inspect the plant"}</h3>{selection?<ul className="mt-2 space-y-1 text-[12px] text-[#555]">{selection.details.map(d=><li key={d}>• {d}</li>)}</ul>:<p className="mt-1 text-[12px] leading-5 text-[#666]">Select a leaf, branch, flower, or the roots. Every visual signal is also available here as text.</p>}</div>{selection&&<button onClick={onClose} className="text-[11px] font-bold text-[#0A66C2]">Clear</button>}</div></section>}
