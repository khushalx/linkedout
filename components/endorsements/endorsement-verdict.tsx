import { CheckCircle2,FileQuestion } from "lucide-react";import type { EndorsementVerdict as Verdict } from "@/lib/endorsements/types";import { VerificationMeter } from "./verification-meter";
export function EndorsementVerdict({verdict,demo}:{verdict:Verdict;demo?:boolean}){return <section className="rounded-lg border border-[#d7d7d2] bg-[#fafafa] p-4"><div className="flex items-start justify-between gap-3"><div><p className="text-[9px] font-bold uppercase tracking-[.16em] text-[#666]">Cross-examination result</p><h3 className="mt-1 text-lg font-semibold">{verdict.verdictTitle}</h3></div><div className="flex flex-col items-end gap-1"><span className={`rounded border px-2 py-1 text-[9px] font-black uppercase ${verdict.confidenceScore>=65?"border-[#8fbd9f] bg-[#eff8f2] text-[#356744]":"border-[#d5b1a6] bg-[#fff5f2] text-[#8c3d25]"}`}>{verdict.badge}</span>{demo&&<span className="text-[8px] font-bold text-[#777]">DEMO REVIEW</span>}</div></div><div className="mt-4"><VerificationMeter value={verdict.confidenceScore}/></div><p className="mt-4 text-[12px] leading-5 text-[#555]">{verdict.reasoning}</p><div className="mt-4 grid gap-3 sm:grid-cols-2"><div><p className="mb-2 flex items-center gap-1 text-[10px] font-bold uppercase text-[#057642]"><CheckCircle2 size={13}/>Evidence found</p>{verdict.evidenceFound.map(x=><p key={x} className="mb-1 text-[11px]">• {x}</p>)}{!verdict.evidenceFound.length&&<p className="text-[11px] text-[#777]">No specific evidence recorded.</p>}</div><div><p className="mb-2 flex items-center gap-1 text-[10px] font-bold uppercase text-[#B24020]"><FileQuestion size={13}/>Could strengthen</p>{verdict.missingEvidence.map(x=><p key={x} className="mb-1 text-[11px]">• {x}</p>)}{!verdict.missingEvidence.length&&<p className="text-[11px] text-[#777]">The record is unusually complete.</p>}</div></div></section>}
