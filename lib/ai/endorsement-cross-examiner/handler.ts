import { runStructured } from "../groq-client";import { endorsementVerdictSchema } from "@/lib/endorsements/schema";import { endorsementFallback } from "@/lib/endorsements/fallback";import type { ProfileSkill,SkillEndorsement } from "@/lib/endorsements/types";
const prompt=`You are a careful professional evidence reviewer with light courtroom humour. Evaluate only supplied endorsement testimony and profile evidence. Reward direct experience, specific contributions, named projects and useful evidence. Public work can be valid; certificates are not automatically stronger than project work. Never call anyone dishonest, claim objective truth, or invent evidence. Use supported, partially supported, vague, or unverified language. Return JSON matching the supplied result schema with integer scores 0-100 and arrays of at most five items.`;
export async function handleEndorsementCrossExaminer(skill:ProfileSkill,endorsement:SkillEndorsement){const fallback=()=>endorsementFallback(endorsement,skill);if(!process.env.GROQ_API_KEY)return{result:fallback(),demo:true};try{return{result:await runStructured(prompt,{skill,endorsement},endorsementVerdictSchema,.35),demo:false}}catch{return{result:fallback(),demo:true}}}
